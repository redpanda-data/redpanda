# Redpanda `protoc` plugin

If you're looking for documentation about writing protobufs, you're probably looking for the README in `//proto/redpanda`

Otherwise this directory contains a protoc plugin written in go that generates Redpanda friendly C++ protocol buffer implementations.
