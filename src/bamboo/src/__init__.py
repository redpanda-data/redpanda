# bamboo is the end-to-end testing framework for redpanda
# we use docker-py to orchestrate .tar.gz install files
# into docker containers with their own network.
#
# The idea is to be able to launch multiple chaos tests locally
