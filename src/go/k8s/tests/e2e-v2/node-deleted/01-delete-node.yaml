apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: kubectl -n redpanda get pod node-deleted-1 -o jsonpath='{.spec.nodeName}' | xargs kubectl delete node
assert:
  - 01-assert-delete-node.yaml
