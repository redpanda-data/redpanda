apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: kubectl -n redpanda get pod node-deleted-2 -o jsonpath='{.spec.nodeName}' | xargs kubectl delete node
assert:
  - 02-assert-delete-node-again.yaml
