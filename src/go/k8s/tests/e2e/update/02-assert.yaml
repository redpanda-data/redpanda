apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: update-cluster
spec:
  template:
    spec:
      containers:
        - resources:
            requests:
              memory: 89M
        - resources:
            requests:
              memory: 10M
