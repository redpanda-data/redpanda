apiVersion: redpanda.vectorized.io/v1alpha1
kind: Cluster
metadata:
  name: update-cluster
spec:
  configuration:
    kafkaApi:
      - name: kafka
        port: 9092
        tls:
          enabled: true