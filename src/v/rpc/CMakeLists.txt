v_cc_library(
  NAME rpc
  SRCS
    types.cc
    netbuf.cc
    transport.cc
    reconnect_transport.cc
    connection_cache.cc
    rpc_server.cc
  DEPS
    Seastar::seastar
    v::bytes
    v::rphashing
    v::utils
    v::reflection
    absl::flat_hash_map
    v::compression
    v::net
  )
add_subdirectory(test)
