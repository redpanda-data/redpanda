# coding: utf-8

"""
Redpanda Schema Registry API

No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: 1.1.1
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self


class SecurityAcl(BaseModel):
    """
    SecurityAcl
    """  # noqa: E501

    principal: StrictStr = Field(
        description='The name of the principal, for example, User:alice or RedpandaRole:admin. Use "*" to represent a wildcard.'
    )
    resource: StrictStr = Field(
        description='The name of the resource. Use "*" to represent a wildcard.'
    )
    resource_type: StrictStr = Field(description="The type of resource being secured.")
    pattern_type: StrictStr = Field(
        description="Pattern match type for the resource. Only applies when `resource_type` is SUBJECT."
    )
    host: StrictStr = Field(
        description='Originating host for which this rule applies. Use "*" to represent a wildcard.'
    )
    operation: StrictStr = Field(description="The operation to allow or deny.")
    permission: StrictStr = Field(
        description="Specifies whether the operation is allowed or denied."
    )
    __properties: ClassVar[List[str]] = [
        "principal",
        "resource",
        "resource_type",
        "pattern_type",
        "host",
        "operation",
        "permission",
    ]

    @field_validator("resource_type")
    def resource_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["REGISTRY", "SUBJECT"]):
            raise ValueError("must be one of enum values ('REGISTRY', 'SUBJECT')")
        return value

    @field_validator("pattern_type")
    def pattern_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["LITERAL", "PREFIXED"]):
            raise ValueError("must be one of enum values ('LITERAL', 'PREFIXED')")
        return value

    @field_validator("operation")
    def operation_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(
            [
                "ALL",
                "READ",
                "WRITE",
                "DELETE",
                "DESCRIBE",
                "DESCRIBE_CONFIGS",
                "ALTER_CONFIGS",
            ]
        ):
            raise ValueError(
                "must be one of enum values ('ALL', 'READ', 'WRITE', 'DELETE', 'DESCRIBE', 'DESCRIBE_CONFIGS', 'ALTER_CONFIGS')"
            )
        return value

    @field_validator("permission")
    def permission_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["ALLOW", "DENY"]):
            raise ValueError("must be one of enum values ('ALLOW', 'DENY')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SecurityAcl from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SecurityAcl from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "principal": obj.get("principal"),
                "resource": obj.get("resource"),
                "resource_type": obj.get("resource_type"),
                "pattern_type": obj.get("pattern_type"),
                "host": obj.get("host"),
                "operation": obj.get("operation"),
                "permission": obj.get("permission"),
            }
        )
        return _obj
